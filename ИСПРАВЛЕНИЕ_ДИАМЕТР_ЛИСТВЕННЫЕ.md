# Добавление поля диаметра для лиственных пород

## Проблема

В меню ввода пород **для лиственных пород отсутствовало поле для ввода диаметра**, соответственно диаметр не сохранялся и не участвовал в расчётах.

### Было:

**Для хвойных пород:**
- До 0.5м (шт)
- 0.5-1.5м (шт)
- >1.5м (шт)
- Высота (м)
- Густота (авто)
- Возраст (лет)
- ❌ **Диаметр (см)** - ОТСУТСТВОВАЛ

**Для лиственных пород:**
- Густота (шт)
- Высота (м)
- Возраст (лет)
- ❌ **Диаметр (см)** - ОТСУТСТВОВАЛ

## Решение

Добавлено поле **"Диаметр (см)"** для обоих типов пород.

### Изменения в `molodniki_extended.py`

#### 1. Добавлено поле в форму ввода (строка 2150-2166)

```python
if breed_type == 'coniferous':
    fields = [
        ('До 0.5м (шт):', 'do_05'),
        ('0.5-1.5м (шт):', '05_15'),
        ('>1.5м (шт):', 'bolee_15'),
        ('Высота (м):', 'height'),
        ('Диаметр (см):', 'diameter'),  # ✅ ДОБАВЛЕНО
        ('Густота (авто):', 'density'),
        ('Возраст (лет):', 'age')
    ]
else:
    fields = [
        ('Густота (шт):', 'density'),
        ('Высота (м):', 'height'),
        ('Диаметр (см):', 'diameter'),  # ✅ ДОБАВЛЕНО
        ('Возраст (лет):', 'age')
    ]
```

#### 2. Обработка диаметра как float (строка 2257-2262)

```python
for key, inp in self.breed_inputs.items():
    if inp.text.strip():
        try:
            if key in ['density', 'age']:
                breed_data[key] = int(inp.text)
            elif key in ['height', 'diameter']:  # ✅ ДОБАВЛЕНО
                breed_data[key] = float(inp.text)
            else:
                breed_data[key] = float(inp.text)
        except ValueError:
            breed_data[key] = 0
```

#### 3. Отображение диаметра в списке пород (строка 2595-2596)

```python
# Для лиственных пород
if 'diameter' in breed_info and breed_info['diameter']:
    params_text.append(f"Диаметр: {breed_info['diameter']}см")
```

## Интерфейс

### Ввод данных для лиственных пород:

```
┌─────────────────────────────────┐
│  Лиственная порода: Берёза      │
├─────────────────────────────────┤
│  Густота (шт):     [_____]      │
│  Высота (м):       [_____]      │
│  Диаметр (см):     [_____]  ✅  │
│  Возраст (лет):    [_____]      │
├─────────────────────────────────┤
│  [Добавить] [Удалить] [Отмена]  │
└─────────────────────────────────┘
```

### Отображение в списке пород:

**Хвойные:**
```
1. Сосна (Хвойная)
   До 0.5м: 100; 0.5-1.5м: 200; >1.5м: 150
   Высота: 2.0м; Диаметр: 4.0см; Возраст: 15 лет
```

**Лиственные:**
```
2. Берёза (Лиственная)
   Густота: 5000; Высота: 6.0м; Диаметр: 5.0см; Возраст: 20 лет
```

## Структура данных

```json
{
  "name": "Берёза",
  "type": "deciduous",
  "density": 5000,
  "height": 6.0,
  "diameter": 5.0,  ✅
  "age": 20
}
```

## Влияние на расчёты

Теперь диаметр участвует в расчётах:

### 1. Средний диаметр по породам:
```python
avg_diameter = sum(p.get('diameter', 0) for p in data['plots'] if p.get('diameter', 0) > 0) / ...
```

### 2. Средний диаметр по участку:
```python
avg_overall_diameter = sum(p['diameter'] for p in plot_data_list) / num_plots
```

### 3. Проектный диаметр (после рубки):
```python
project_diameter = diameter * (1 + intensity/100 * 0.3)
```

## Проверка

### 1. Ввод данных:
1. Откройте молодняки
2. Добавьте площадку
3. Выберите "Лиственные" → "Берёза"
4. Введите: Густота=5000, Высота=6.0, **Диаметр=5.0**, Возраст=20
5. Нажмите "Добавить"

### 2. Проверка в списке:
Убедитесь, что в списке пород отображается:
```
Берёза (Лиственная)
Густота: 5000; Высота: 6.0м; Диаметр: 5.0см; Возраст: 20 лет
```

### 3. Проверка в JSON:
```bash
python check_diameter_in_json.py
```

**Ожидаемый результат:**
```
Берёза:
   Диаметр: 5.0 см ✅
```

### 4. Проверка в проекте ухода:
Откройте заполненный проект и проверьте таблицу "Характеристика насаждения по породам":
- Столбец "Диаметр исх" должен быть заполнен для лиственных пород

## Файлы с изменениями

| Файл | Строки | Изменения |
|------|--------|-----------|
| `molodniki_extended.py` | 2150-2166 | Добавлено поле `diameter` в форму |
| `molodniki_extended.py` | 2257-2262 | Обработка `diameter` как float |
| `molodniki_extended.py` | 2595-2596 | Отображение в списке пород |

## Примечания

- Диаметр вводится в сантиметрах (см)
- Допустимый диапазон: 0.0-150.0 см
- Поле необязательное (можно оставить пустым)
- Для хвойных пород диаметр также добавлен
